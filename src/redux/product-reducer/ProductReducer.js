const initialState = {
    products : [],
    cartData : []
}

//reducer is the one  which will update the store
export const ProductReducer = (state = initialState,action)=>{
   switch(action.type){

    default : 
    return state;
   }
}